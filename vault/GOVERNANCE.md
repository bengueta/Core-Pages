# Governance (vault-pages template)

**Deploy:** only after this folder is copied into **Core-Pages** — see INTEGRATE_CORE_PAGES.md.

**Branch protection:** configure in **Core-Pages** on the default branch (PR + required checks).

**CODEOWNERS:** use CODEOWNERS.example at the **Core-Pages** repo root when ready.

**Secrets:** no Doppler/CoreStrategy keys. Giscus IDs optional via that repo Actions only.
